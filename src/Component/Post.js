import React from 'react';
import './Post.css'

const Post = ({profilePic, image, userName, timeStamp, message}) => {
    return (
        <div className="post">
            
        </div>
    );
};

export default Post;